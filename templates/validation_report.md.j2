# Render-vs-Data-Model Validation Report

Generated by `ansible-playbook playbooks/00_validate_render.yml`, which
wraps [`scripts/validate_configs.py`](../scripts/validate_configs.py) --
confirms the CLI in `rendered_configs/<hostname>.cfg` actually matches what
`physical_topology.yaml` / `logical_topology.yaml` / the platform role
baseline say it should, using the exact same lookup functions
(`device_phys_links`, `device_routing_record`, `wan_peer_binding`,
`ios_addr`, ...) the Jinja2 templates themselves call -- a check failure
here means the templates/filters combination has a genuine defect, not
that this report disagrees with the templates for its own reasons.

Generated: {{ lookup('pipe', 'date') }}

## Summary

| Metric | Count |
|---|---|
| Devices checked | {{ validation_data.summary.device_count }} |
| Checks passed | {{ validation_data.summary.total_pass }} |
| Checks failed | {{ validation_data.summary.total_fail }} |
| Known-gap skips | {{ validation_data.summary.skipped }} |

*SKIP = a known, already-documented data-model gap (e.g. a WAN router's
eBGP peer with no derivable local IP -- FINDING 1 in
[logical topology.j2](../templates/logical%20topology.j2)'s header) --
intentionally skipped rather than failed, since failing it would just be
re-reporting a gap this repo already documents elsewhere, not a new render
defect.
{% if validation_data.summary.total_fail > 0 %}

## FAILURES

{% for device in validation_data.devices %}
{% for check in device.checks %}
{% if not check.passed %}
- **[{{ device.name }}] {{ check.category }}** -- expected `{{ check.expectation }}`, not found in rendered config
{% endif %}
{% endfor %}
{% endfor %}
{% endif %}

## Per-Device Detail

Every row below is one assertion this run made against
`rendered_configs/<hostname>.cfg` -- exactly what was tested, not just the
pass/fail count.

{% for device in validation_data.devices %}
### {{ device.name }}

| Category | Expectation | Result |
|---|---|---|
{% for check in device.checks %}
| {{ check.category }} | `{{ check.expectation }}` | {{ 'SKIP' if (check.passed and check.note) else ('PASS' if check.passed else 'FAIL') }}{{ ' -- ' + check.note if check.note else '' }} |
{% endfor %}

{% endfor %}
