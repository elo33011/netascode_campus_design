---
# Install with: ansible-galaxy collection install -r requirements.yml
collections:
  - name: cisco.ios
    version: ">=5.0.0"
  - name: cisco.nxos
    version: ">=5.0.0"
  - name: ansible.netcommon
    version: ">=5.0.0"
  - name: ansible.utils
    version: ">=3.0.0"
